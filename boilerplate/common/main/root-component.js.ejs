
<% if (props.i18n === true) { %>import '../i18n'<% } %>
import React from 'react'
import { Text } from 'react-native'

/**
 * This is the root component of our app.
 */
export class RootComponent extends React.Component {
  render () {
    return (
      <Text> Hello! </Text>
    )
  }
}
