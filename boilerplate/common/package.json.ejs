{
  "version": "0.0.1",
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "lint": "prettier-standard 'src/**/*.js'",
    "precommit": "lint-staged",
    "storybook": "storybook start -p 7007"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "ramda": "0.25.0",
    <% if (props.i18n === true) { %> "react-native-i18n": "2.0.12", <% } %>
    "react-native-keychain": "3.0.0"
  },
  "devDependencies": {
    <% if (props.storybooks === true) { %>
    "@storybook/addon-actions": "^4.0.2",
    "@storybook/addon-links": "^4.0.2",
    "@storybook/addons": "^4.0.2",
    "@storybook/react-native": "^3.4.0",
    <% } %>
    <% if (props.linter === true) { %>
      "prettier-standard": "^8.0.1", 
      "husky": "0.14.3",
      "lint-staged": "7.1.0",
    <% } %>
    "npm-run-all": "4.1.3",
    "react-native-dotenv": "^0.2.0",
    "reactotron-react-native": "2.1.0"
  },
  "rnpm": {
    "assets": [
      "./src/theme/fonts/"
    ]
  } <% if (props.linter === true) { %> ,
  "standard": {
    "globals": [
      "__DEV__",
      "beforeAll",
      "afterAll",
      "test",
      "expect",
      "describe"
    ]
  },
  "lint-staged": {
    "*.{js,jsx}": ["prettier-standard src/**/*.js", "git add"],
    "*.md": ["prettier-standard src/**/*.md", "git add"],
    "*.json": ["prettier-standard src/**/*.json", "git add"]
  }
 <% } %>
}
